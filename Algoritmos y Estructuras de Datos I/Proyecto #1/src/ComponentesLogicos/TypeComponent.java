package ComponentesLogicos;

/**
 * Enum para enumerar los tipos de componentes
 * existentes.
 */
public enum TypeComponent {
    AND,
    NAND,
    OR,
    NOR,
    NOT,
    XOR,
    XNOR,
    OTHER
}